# Define propedeutic variables for WeGetFinancing WooCommerce Checkout Plugin

export PROJ_FOLDER="$PWD"
export SVN_FOLDER="${HOME}/svn"

# Load local overrides if present
if [[ -f .envrc.local ]]; then
  source_env ".envrc.local"
fi

